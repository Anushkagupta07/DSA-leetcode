class Solution {
public:
    int minBitFlips(int start, int goal) {
    int ans = start ^ goal;
    int count = 0;
    while(ans > 1){
        if(ans & 1) count+=1;
        ans = ans>>1;
    }   
    if(ans==1) count+=1;
    return count;
    }
};
//t.c. = O(logbase2n)
//s.c. = O(1)
